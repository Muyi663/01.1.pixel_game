# Pixel Art Quiz Game (React + Google Sheets)

一个基于 React Vite 和 Google Sheets 的像素风闯关问答游戏。

## 🚀 快速开始 (Quick Start)

### 1. 前端安装与运行
确保你已安装 Node.js。

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

### 2. 环境变量配置
复制 `.env` 文件并填入你的 Google Apps Script URL。

```ini
VITE_GOOGLE_APP_SCRIPT_URL=你的_SCRIPT_URL
VITE_PASS_THRESHOLD=3
VITE_QUESTION_COUNT=5
```

---

## 📊 Google Sheets 配置 (后端数据库)

请建立一个新的 Google Spreadsheet，并创建以下两个 **工作表 (Sheets)**：

### 工作表 1: `Questions` (题目库)
请务必严格参照以下列顺序（第一行是标题）：

| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| **No** | **Question** | **A** | **B** | **C** | **D** | **Answer** |
| 1 | 什么是 LLM? | 大型语言模型 | 柠檬 | 拿铁 | 乐高 | 大型语言模型 |
| 2 | ChatGPT 是谁开发的? | Google | OpenAI | Meta | Apple | OpenAI |

> **注意**：
> - `No` 是题号，请按数字排列。
> - `Answer` 字段必须与选项内容 **完全一致**（字面匹配）。

### 工作表 2: `Responses` (玩家记录)
用于存储玩家成绩。请设置以下标题列：

| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| **ID** | **闯关次数** | **总分** | **最高分** | **第一次通关分数** | **花了几次通关** | **最近游玩时间** |

---

## ⚙️ Google Apps Script 配置 (API 服务)

1. 在你的 Google Sheet 中，点击菜单栏的 **Extensions (扩展程序)** > **Apps Script**.
2. 清空 `Code.gs` 中的默认代码。
3. 将本项目根目录下的 `backend_script.gs` 文件内容完整复制贴上。
4. 点击 **Deploy (部署)** > **New deployment (新建部署)**.
5. 配置如下：
   - **Select type**: Web app
   - **Description**: v1
   - **Execute as**: `Me (your email)` (以我身分执行)
   - **Who has access**: `Anyone` (任何人) **<-- 这里很重要！否则前端无法跨域调用**
6. 点击 **Deploy**，复制生成的 `Web app URL` (以 `/exec` 结尾)。
7. 将此 URL 填入你的前端 `.env` 文件的 `VITE_GOOGLE_APP_SCRIPT_URL` 字段。

---
